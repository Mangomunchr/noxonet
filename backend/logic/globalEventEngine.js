function getCurrentEvent() {
  return {
    active: true,
    name: "Chaos Eclipse: Double XP Weekend",
    description: "All XP gains are doubled. Vault rituals grant bonus credits."
  };
}

module.exports = { getCurrentEvent };